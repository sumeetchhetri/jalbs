
```
//Process Entry point
function (start)
{
	for(i=1;i<100;i++)
	{
		out(i)
	}

        //Inter-Process communication
        //KILL - send a KILL signal to process with id 2
        //bye bye - message sent along
	sendMsg(2,KILL,bye bye)
}
```